//Criando o tema padrão para o aplicativo
import 'package:flutter/material.dart';

var colorScheme = ColorScheme.fromSeed(seedColor: Colors.greenAccent);
final ThemeData themeData = ThemeData(colorScheme: colorScheme);
